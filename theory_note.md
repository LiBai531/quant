# Margin 收缩的投影理论（研究笔记 v1）

日期：2026-08-18 ｜ 模型：Qwen3.5-4B（本地 CPU 诊断，3-bit RTN，gs=128，6×512 tok）

## 1. 定律

量化对决策边际（margin）的作用是乘性信道：

    m̃ = c·m + b + ε,    flip ⟺ sign 翻转

- 首次观测：arXiv:2608.06564（大 margin 人工决策制域，m~17）。
- 本地复现（小 margin 自然文本制域，m~1.8）：**c = 0.7426 ± 0.0241（z=10.7），加性被拒绝**。
  独立证据：m>6 的位置仍有 10.6% 翻转（加性模型预言 ≈0）。

## 2. 推导（每步的地位）

**Step 1（逐格代数，精确）**：RTN 把权重换成格点中心 = 条件期望 = L² 投影。
对格内求和：Σ δ·w = −Σ δ² < 0 ⟹ **量化误差与权重本身必然负相关**。

**Step 2（定义即得）**：沿任何线性通路（特征方向 v → 读出方向 u），
有效增益不是被加噪声扰动，而是系统性缩小 γ = 通路噪声能量/信号能量
= ‖Ŵ−W‖²/‖W‖²（按通路口径加权）。

**Step 3（唯一实质假设，可检验）**：决策 margin 骑在该通路上，m ∝ 增益。

联立：**c = 1 − γ_pathway**，b = 基线（不经通路的分量）读出，ε = 旁路噪声。

## 3. 零参数预测与实测

    c_pred = 1 − γ_head − Σ_l ρ_l·γ_l
    （γ: 实际量化权重的能量比；ρ_l: 块 l 对残差流的能量贡献份额，无任何拟合参数）

| 量 | 值 |
|---|---|
| c_fit（3-bit all） | 0.7426 ± 0.0241 |
| c_pred | 0.8463（γ_head=0.086, γ_body=0.068） |
| 命中率 | 理论解释收缩的 60%（0.154 / 0.257） |

误差来源的实测线索：一阶分解中 cross 项 median=0.41（propa 的 28%）——
3-bit 下 Δh 大，一阶展开开始失效，二阶项吃掉预测精度。

## 4. 可证伪预言（排队实验中）

1. **4-bit 下 |c_fit − c_pred| 应收窄至 <0.03**（一阶更准、cross 更小）；
   不收窄 ⟹ 理论有结构性问题。
2. c(4-bit) > c(3-bit)，可描出 c(b) 塌缩曲线，与 2608.06564 的高 margin 制域对比。
3. scope=head：c_pred = 1 − γ_head 单独检验 head 通路；
   scope=body：c_pred = 1 − Σρ_l γ_l 单独检验 body 通路。

## 5. 附带结论（3-bit all 诊断）

- flip 率 54.1%，远端翻转 38.6%：已越出近邻竞争区。
- 误差分解：propa/direct 中位比 = 2.3（body 传播主导）。
- 异方差：‖h‖ 四分位 Q1→Q4 flip 率 39%→68%，单调，温和。
- b = +0.26：本模型/语料上 gap 被正向推。

## 6. 对最终目标（量化算法 + token 对齐）的含义

Token 对齐 = Φ(−(cm+b)·sign(m)/σ)。固定 bit 下唯一杠杆是 **γ 工程**：
- SINQ 式平衡 → 降 Δ/σ → 降 γ → 升 c（SINQ 由此获得理论）。
- 按通路能量比分配 bit（γ 大的层多给）。
- c 可从权重统计先验计算 ⟹ 先算后量，不跑量化就知道会塌在哪。

## 参考文献

- arXiv:2608.06564 — 乘性定律原始观测（大 margin 制域）
- arXiv:2507.18553 — GPTQ = Babai 最近平面（格论视角）
- arXiv:2607.07964 — KronQ，输出侧 Hessian（Fisher 加权的实例）
- 工具：quant/flip_diagnosis.py（分解 + 乘性检验 + γ 预测一体）
